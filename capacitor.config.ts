import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.storex.app',
  appName: 'StoreX',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
